import styled from "styled-components";
import { lightGray } from "../../utils/themeColors";

export default styled.p`
    color: ${lightGray};
    font-size: 1rem;
    font-weight: 400;
    letter-spacing: 1px;
`;